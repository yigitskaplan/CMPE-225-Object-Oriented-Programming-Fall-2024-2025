#include <iostream>
#include <fstream>
#include <cctype>

using namespace std;

int main(){

    ifstream fin("input.txt");

    int upper = 0, lower = 0, blank = 0;
    int word = 0, lines = 0, char = 0;
    string line;

    while(getline(fin, line)){
        lines++;

        if 
    }
}